{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the bundled local Linkdqueue window only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
